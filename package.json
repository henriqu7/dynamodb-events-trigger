{
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "aws-sdk": "^2.875.0"
  },
  "scripts": {
    "invoke-local": "sls invoke local -f hero-insert --path requests-mock/hero.insert.json --logs",
    "invoke": "sls invoke -f hero-insert --path requests-mock/hero.insert.json --logs",
    "invoke-trigger": "sls invoke local -f hero-trigger --path requests-mock/hero.trigger.json"
  }
}
